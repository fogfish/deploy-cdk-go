name: spawner-cdk-go

description: 'xxx'

author: "xxx"

branding:
  icon: "alert-triangle"
  color: "blue"

inputs:
  stack:
    description: |
      xxx
    required: true

runs:
  steps:
    - name: go get tools
      run: |
        echo "==> ${{ inputs.stack }}"
